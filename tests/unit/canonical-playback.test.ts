import {describe,it,expect} from 'vitest'; import {canonicalTitleId} from '../../src/core/identity/canonical-id.js';
describe('canonical playback consistency',()=>{it('generates one deterministic title ID for all API projections',()=>{expect(canonicalTitleId('THE MARCH ON WASHINGTON',undefined,'movie')).toBe('title_e5a47953e8ba9a1d32381813');expect(canonicalTitleId('THE MARCH ON WASHINGTON',undefined,'movie')).toBe(canonicalTitleId('THE MARCH ON WASHINGTON',undefined,'movie'))});it('preserves official media metadata shape',()=>{const source={duration:1226.83,resolution:'320x240',format:'mp4',mimeType:'video/mp4'};expect(source).toMatchObject({duration:1226.83,resolution:'320x240',format:'mp4',mimeType:'video/mp4'})})});
