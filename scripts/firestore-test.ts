import {FirestoreCatalogRepository} from '../src/repositories/firestore-catalog.repository.js';
const repo=new FirestoreCatalogRepository();
const title={titleId:'title_naruto',title:'Naruto',originalTitle:'ナルト',alternateTitles:['Naruto'],description:'Metadata-only catalog fixture for API and Firestore integration testing. No copyrighted playback source is included.',year:2002,type:'anime',status:'active',availableAudioLanguages:[],subtitleLanguages:[],providers:['test-catalog'],seasons:[{seasonId:'season_naruto_1',seasonNumber:1,episodes:[]}],sources:[]};
const before=await repo.getTitle(title.titleId);await repo.upsertTitle(title);const written=await repo.getTitle(title.titleId);if(JSON.stringify(written)!==JSON.stringify(title))throw new Error('Firestore title_naruto read-back mismatch');const updated={...title,description:`${title.description} Updated.`};await repo.upsertTitle(updated);const readUpdated=await repo.getTitle(title.titleId);if(readUpdated?.description!==updated.description)throw new Error('Firestore update mismatch');await (await import('../src/config/firebase.js')).firebaseAdmin().firestore.collection('titles').doc(title.titleId).delete();const cleaned=await repo.getTitle(title.titleId);if(cleaned!==null)throw new Error('Firestore cleanup failed');console.log(JSON.stringify({success:true,beforeExists:Boolean(before),written,updated:readUpdated,cleaned:cleaned===null},null,2));
