import {env} from '../src/config/env.js'; const checks=[['environment',Boolean(env.NODE_ENV)],['node',Number(process.versions.node.split('.')[0])>=20],['local storage',Boolean(env.LOCAL_STORE_FILE)],['admin auth',Boolean(env.ADMIN_API_KEY)]];let fail=0;for(const [n,p] of checks){console.log(`${p?'PASS':'FAIL'} ${n}`);if(!p)fail++}process.exit(fail?1:0);
