import {z} from 'zod';
export const env=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),PORT:z.coerce.number().default(3000),LOCAL_STORE_FILE:z.string().default('./data/catalog.json'),STORAGE_MODE:z.enum(['local','firestore']).default('local'),JWT_SECRET:z.string().min(16).default('development-only-secret-please-change'),ADMIN_API_KEY:z.string().default('development-admin-key'),CORS_ORIGIN:z.string().default('*'),PROVIDER_TIMEOUT_MS:z.coerce.number().int().min(1000).max(30000).default(8000),CACHE_TTL_SECONDS:z.coerce.number().int().min(60).default(21600),PEERTUBE_INSTANCES:z.string().default('https://peertube2.cpy.re,https://fedi.video'),DVIDS_API_KEY:z.string().default(''),NARA_API_KEY:z.string().default('')}).parse(process.env);
