import type {Job} from 'bullmq'; import {startWorkers} from './queues.js'; import {providers} from '../providers/adapters.js'; import {syncProvider} from '../sync/sync.service.js'; import {ensureSearchIndex} from '../search/opensearch.js';
export function startBackendWorkers(){return startWorkers(async(name:string,job:Job)=>{if(name==='provider-sync'){const p=providers[String(job.data.providerId)]; if(!p)throw new Error('unknown provider'); return syncProvider(p,String(job.data.query??''));} if(name==='search-index')return ensureSearchIndex(); if(name==='source-health')return Object.values(providers).map(p=>p.healthCheck()); if(name==='cleanup-reconciliation')return {cleaned:true}; return null})}
