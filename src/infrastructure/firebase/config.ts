import {z} from 'zod';
export const firebaseConfig=z.object({apiKey:z.string().default(''),authDomain:z.string().default(''),projectId:z.string().default(''),storageBucket:z.string().default(''),messagingSenderId:z.string().default(''),appId:z.string().default('')}).parse({apiKey:process.env.FIREBASE_API_KEY,authDomain:process.env.FIREBASE_AUTH_DOMAIN,projectId:process.env.FIREBASE_PROJECT_ID,storageBucket:process.env.FIREBASE_STORAGE_BUCKET,messagingSenderId:process.env.FIREBASE_MESSAGING_SENDER_ID,appId:process.env.FIREBASE_APP_ID});
export const firebaseConfigured=Boolean(firebaseConfig.projectId && firebaseConfig.appId);
