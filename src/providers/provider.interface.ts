export type ProviderCapabilities={search:boolean;metadata:boolean;episodes:boolean;playback:boolean;hls:boolean;mp4:boolean;subtitles:boolean;webhooks:boolean};
export type ProviderResult={providerId:string;externalId:string;title:string;originalTitle?:string;year?:number;mediaType:'movie'|'series'|'documentary'|'other';licenseStatus:'public-domain'|'open-license'|'authorized'|'unknown';url?:string;aliases?:string[]};
export interface ProviderAdapter{readonly id:string;readonly name:string;readonly capabilities:ProviderCapabilities; search(query:string):Promise<ProviderResult[]>; getTitle(id:string):Promise<ProviderResult|null>; getSeasons(titleId:string):Promise<unknown[]>; getEpisodes(seasonId:string):Promise<unknown[]>; getMedia(id:string):Promise<unknown[]>; resolvePlayback(sourceId:string):Promise<string|null>; getUpdates(cursor?:string):Promise<ProviderResult[]>; healthCheck():Promise<{status:'healthy'|'degraded'|'unhealthy';latency:number}>;}
